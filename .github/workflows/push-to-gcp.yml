name: Push to GCP
on:
  push:
    branches:
      - 'develop'
      - 'master'
jobs:
  build:
    name: Build and Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Configure GCP Credentials
        uses: docker/login-action@v1
        with:
          registry: us-central1-docker.pkg.dev
          username: _json_key
          password: ${{ secrets.GAR_JSON_KEY }}
      - name: Push to GCP Artifacts Registry
        id: build-images
        env:
          IMAGE_TAG: ${{ github.sha }}
          REPOSITORY_BASE: us-central1-docker.pkg.dev/gochat-demo
        run: |
          # Build the Docker Images
          docker build -t $REPOSITORY_BASE/gochat-service-accounts/gochat-service-accounts:$IMAGE_TAG -t $REPOSITORY_BASE/gochat-service-accounts/gochat-service-accounts:${GITHUB_REF##*/} -f platform/services/account/Dockerfile .
          docker push $REPOSITORY_BASE/gochat-service-accounts/gochat-service-accounts --all-tags
          docker build -t $REPOSITORY_BASE/gochat-service-conversations/gochat-service-conversations:$IMAGE_TAG -t $REPOSITORY_BASE/gochat-service-conversations/gochat-service-conversations:${GITHUB_REF##*/} -f platform/services/conversation/Dockerfile .
          docker push $REPOSITORY_BASE/gochat-service-conversations/gochat-service-conversationss --all-tags
          docker build -t $REPOSITORY_BASE/gochat-service-api/gochat-service-api:$IMAGE_TAG -t $REPOSITORY_BASE/gochat-service-api/gochat-service-api:${GITHUB_REF##*/} -f platform/endpoints/api/Dockerfile .
          docker push $REPOSITORY_BASE/gochat-service-api/gochat-service-api --all-tags
          docker build -t $REPOSITORY_BASE/gochat-service-ws/gochat-service-ws:$IMAGE_TAG -t $REPOSITORY_BASE/gochat-service-ws/gochat-service-ws:${GITHUB_REF##*/} -f platform/endpoints/ws/Dockerfile .
          docker push $REPOSITORY_BASE/gochat-service-ws/gochat-service-ws --all-tags
          docker build -t $REPOSITORY_BASE/gochat-service-frontend/gochat-service-frontend:$IMAGE_TAG -t $REPOSITORY_BASE/gochat-service-frontend/gochat-service-frontend:${GITHUB_REF##*/} -f frontend/Dockerfile .
          docker push $REPOSITORY_BASE/gochat-service-frontend/gochat-service-frontend --all-tags
